services:
  backend:
    image: ghcr.io/omega-force-cyber-monk/christinacormie-backend:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: christinacormie-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${PORT:-3000}:3000'
